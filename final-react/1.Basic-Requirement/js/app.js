const points = [2,4,6,7,234,55,6];

const newPoints = [...points,"aaa"];

// let total = 0;

// const uniquePoints = new Set(points);

// for ( let point of points){
//     // uniquePoints.push(point);
//     if(!uniquePoints.includes(point)){
//         uniquePoints.push(point);
//     }
// }

// points.push("aaa")
// points.push("bbb")
// points.push("ccc")


console.log(points);
console.log(newPoints);

// total += points[0];
// total += points[1];
// total += points[2];
// total += points[3];

// for (let point of points) {
//     total += point;
// }

// console.log(total);

// console.log(points.reduce((pv,cv) => pv + cv, 0));object